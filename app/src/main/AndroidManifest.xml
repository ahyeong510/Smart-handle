<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 위치 권한 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 인터넷 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 블루투스 -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHandle"
        android:usesCleartextTraffic="true">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCW5MXLusoUzLSFiZGdirbZv7V2h6Zazw0" />

        <!-- 시작 화면 -->
        <activity
            android:name=".ui.start.StartActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartHandle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 권한 화면 -->
        <activity
            android:name=".ui.permission.PermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartHandle" />

        <!-- 메인 화면 -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartHandle" />

        <!-- 일반 주행 화면 -->
        <activity
            android:name=".ui.driving.DrivingActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SmartHandle" />

        <!-- 운동 주행 화면 -->
        <activity
            android:name=".ui.fitness.FitnessDrivingActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartHandle" />

        <!-- 운동 추천 결과 화면 -->
        <activity
            android:name=".ui.fitness.FitnessRecommendResultActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartHandle" />

        <!-- 기록 화면 -->
        <activity
            android:name=".ui.fitness.HistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartHandle" />

        <!-- 목적지 선택 -->
        <activity
            android:name=".mapselect.SelectLocationActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartHandle" />

        <!-- 지도 화면 -->
        <activity
            android:name=".maps.MapsActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartHandle" />

        <!-- 회원가입 / 로그인 -->
        <activity
            android:name=".auth.SignupActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartHandle" />

        <activity
            android:name=".auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartHandle" />

    </application>

</manifest>